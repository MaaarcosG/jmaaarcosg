import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#289406",
    },
    secondary: {
      main: "#FFF",
    },
    background: {
      default: "#333",
      paper: "#333",
    }
  },
});
